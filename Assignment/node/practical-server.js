const http = require( 'http' ); 

const server = http.createServer(
    ( req, res ) => { 
        const url = request.url;
        switch( url ) {
            case '/':
                response.end{
                    `
                    <!doctype html>
                    <html>
                    
                    `

                }
        }
    }
);

server.listen( 3000 );